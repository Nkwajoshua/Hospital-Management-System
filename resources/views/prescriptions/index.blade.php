@extends('layouts.app')
@section('title','Prescriptions | Hospital Management System')
@section('content')
<div class="mb-4"><h1 class="h3 mb-1">Prescriptions</h1><p class="text-secondary mb-0">Doctor prescriptions and pharmacy dispensing.</p></div>
<div class="card border-0 shadow-sm mb-4"><div class="card-body"><form method="GET" class="row g-2"><div class="col-md-10"><select name="status" class="form-select"><option value="">All statuses</option><option value="pending" @selected($status==='pending')>Pending</option><option value="dispensed" @selected($status==='dispensed')>Dispensed</option></select></div><div class="col-md-2 d-grid"><button class="btn btn-outline-primary">Filter</button></div></form></div></div>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Date</th><th>Patient</th><th>Doctor</th><th>Medicine</th><th>Status</th><th></th></tr></thead><tbody>@forelse($prescriptions as $prescription)<tr><td>{{ $prescription->created_at->format('d M Y') }}</td><td>{{ $prescription->patient->first_name }} {{ $prescription->patient->last_name }}<div class="small text-secondary">{{ $prescription->patient->patient_number }}</div></td><td>{{ $prescription->doctor->name }}</td><td>{{ $prescription->items->pluck('medicine.name')->filter()->join(', ') }}</td><td><span class="badge text-bg-{{ $prescription->status==='dispensed'?'success':'warning' }} text-capitalize">{{ $prescription->status }}</span></td><td class="text-end"><a href="{{ route('prescriptions.show',$prescription) }}" class="btn btn-sm btn-outline-primary">View</a></td></tr>@empty<tr><td colspan="6" class="text-center text-secondary py-5">No prescriptions found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $prescriptions->links() }}</div>
@endsection
