@extends('layouts.app')
@section('title','Prescription | Hospital Management System')
@section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><div class="text-secondary">{{ $prescription->patient->patient_number }}</div><h1 class="h3 mb-1">Prescription #{{ $prescription->id }}</h1><p class="text-secondary mb-0">{{ $prescription->patient->first_name }} {{ $prescription->patient->last_name }} · {{ $prescription->doctor->name }}</p></div><a href="{{ route('prescriptions.index') }}" class="btn btn-outline-secondary">Back</a></div>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead class="table-light"><tr><th>Medicine</th><th>Dosage</th><th>Frequency</th><th>Duration</th><th>Quantity</th></tr></thead><tbody>@foreach($prescription->items as $item)<tr><td>{{ $item->medicine->name }}</td><td>{{ $item->dosage }}</td><td>{{ $item->frequency ?: '—' }}</td><td>{{ $item->duration ?: '—' }}</td><td>{{ $item->quantity }}</td></tr>@endforeach</tbody></table></div><div class="card-body d-flex justify-content-between align-items-center"><span class="badge text-bg-{{ $prescription->status==='dispensed'?'success':'warning' }} text-capitalize">{{ $prescription->status }}</span>@if(auth()->user()->role==='pharmacist' && $prescription->status==='pending')<form action="{{ route('prescriptions.dispense',$prescription) }}" method="POST">@csrf @method('PATCH')<button class="btn btn-primary">Dispense Prescription</button></form>@endif</div></div>
@endsection
