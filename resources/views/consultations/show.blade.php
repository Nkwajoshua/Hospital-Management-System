@extends('layouts.app')
@section('title','Medical Record | Hospital Management System')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4"><div><div class="text-secondary">{{ $consultation->patient->patient_number }}</div><h1 class="h3 mb-1">{{ $consultation->patient->first_name }} {{ $consultation->patient->last_name }}</h1><p class="text-secondary mb-0">Consulted by {{ $consultation->doctor->name }} on {{ $consultation->created_at->format('d M Y, h:i A') }}</p></div><div class="d-flex gap-2">@if(auth()->user()->role==='doctor' && $consultation->doctor_id===auth()->id())<a href="{{ route('prescriptions.create',$consultation) }}" class="btn btn-primary">Prescribe Medicine</a><a href="{{ route('consultations.edit',$consultation) }}" class="btn btn-outline-primary">Edit Record</a>@endif<a href="{{ route('consultations.index') }}" class="btn btn-outline-secondary">Back</a></div></div>
<div class="row g-4"><div class="col-lg-8"><div class="card border-0 shadow-sm mb-4"><div class="card-body p-4"><div class="mb-4"><div class="text-secondary small">Complaint / Symptoms</div>{{ $consultation->complaint }}</div><div class="mb-4"><div class="text-secondary small">Diagnosis</div>{{ $consultation->diagnosis }}</div><div class="mb-4"><div class="text-secondary small">Treatment / Plan</div>{{ $consultation->treatment ?: 'Not specified' }}</div><div><div class="text-secondary small">Additional Notes</div>{{ $consultation->notes ?: 'None' }}</div></div></div>
<div class="card border-0 shadow-sm"><div class="card-body"><div class="d-flex justify-content-between align-items-center mb-3"><h2 class="h5 mb-0">Laboratory</h2></div>@if(auth()->user()->role==='doctor' && $consultation->doctor_id===auth()->id())<form action="{{ route('lab-tests.store',$consultation) }}" method="POST" class="d-flex gap-2 mb-3">@csrf<input name="test_name" class="form-control" placeholder="Test name, e.g. Full Blood Count" required><button class="btn btn-outline-primary text-nowrap">Request Test</button></form>@endif@forelse($consultation->labTests as $test)<div class="border rounded p-3 mb-2"><div class="d-flex justify-content-between"><strong>{{ $test->test_name }}</strong><span class="badge text-bg-{{ $test->status==='completed'?'success':'warning' }}">{{ ucfirst($test->status) }}</span></div>@if($test->result)<div class="mt-2">{{ $test->result }}</div>@endif</div>@empty<p class="text-secondary mb-0">No laboratory requests for this consultation.</p>@endforelse</div></div></div>
<div class="col-lg-4"><div class="card border-0 shadow-sm mb-3"><div class="card-body"><h2 class="h6">Linked Appointment</h2><div>{{ $consultation->appointment?->appointment_date?->format('d M Y, h:i A') ?: 'No appointment' }}</div><span class="badge text-bg-success mt-2">Completed</span></div></div><div class="card border-0 shadow-sm"><div class="card-body"><h2 class="h6">Prescriptions</h2>@forelse($consultation->prescriptions as $prescription)<a class="d-block text-decoration-none mb-2" href="{{ route('prescriptions.show',$prescription) }}">Prescription #{{ $prescription->id }} · {{ ucfirst($prescription->status) }}</a>@empty<p class="text-secondary small mb-0">No prescriptions yet.</p>@endforelse</div></div></div></div>
@endsection
